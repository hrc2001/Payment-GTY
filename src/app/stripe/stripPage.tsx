"use client";

import React from "react";

// const   stripProducts = loadStripe(process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY!);

const StripPage = () => {
  return (
    <div>
      <h1>Stripe</h1>
    </div>
  )
}

export default StripPage;
